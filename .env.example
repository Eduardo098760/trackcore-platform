# URL base do Traccar (inclua o protocolo, ex: http://localhost:8082 ou https://demo.traccar.org)
TRACCAR_API_URL=http://localhost:8082

# Se sua instalação do Traccar já expõe endpoints sob o base URL sem o prefixo /api,
# defina TRACCAR_ADD_API=false para testar encaminhar sem adicionar `api/`.
TRACCAR_ADD_API=true

# NEXT_PUBLIC_API_URL é uma alternativa para TRACCAR_API_URL se desejar expor via frontend
NEXT_PUBLIC_API_URL=/api/traccar
